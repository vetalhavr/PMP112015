a=True #змінна для циклу
while a==True: #цикл який повторює виконання програми
 n=int(raw_input('n=')) #задаємо з клавіатури число n 
 sum=0 #початкове значення суми
 for i in range(1,n+1): #цикл, який рахує суму
        p=1 #початкове значення добутку
        for k in range(1,2*i+1): #цикл добутку
          p*=(k+1) #рахуємо добуток
        sum+=p #рахуємо суму з отриманих добутків
 print sum #виводимо суму